{
  "name": "studioquiz",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b game-server",
    "dev-gameserver": "nodemon --watch . --ignore next-app --ext ts --exec \"npm run build\"",
    "start-gameserver": "node dist/game-server/index.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "type-fest": "^4.29.0"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^4.9.0",
    "@socket.io/gcp-pubsub-adapter": "^0.1.0",
    "express": "^4.21.1",
    "socket.io": "^4.8.1"
  }
}
